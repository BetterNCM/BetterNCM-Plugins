name: 'Pack Plugins and Push'

on:
  workflow_dispatch:
  pull_request:
    branches: 
      - main
    types: [closed]
  
    
jobs:
  UpdatePlugins:
    if: (github.event_name == 'pull_request' && github.event.pull_request.merged) || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: 'Set up node 18.x'
        uses: actions/setup-node@v3
        with:
          node-version: 18.x
          cache: 'npm'
      - name: 'Install dependencies'
        run: npm ci
        working-directory: scripts
      - name: 'Run pack script'
        timeout-minutes: 10
        run: node pack-plugins
        id: pack
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
        working-directory: scripts/pack-plugins
      - name: Checkout release repo
        uses: actions/checkout@v3
        with:
          repository: 'BetterNCM/BetterNCM-Packed-Plugins'
          token: ${{ secrets.GH_TOKEN }}
          path: release
      - name: Copy files
        run: |
          cd release
          cp -rf ../tmp ./release
      - name: Commit and push files
        run: |
          git config --local user.email "betterncm-bot@proton.me"
          git config --local user.name "BetterNCM-Bot"
          git remote set-url master https://${{ secrets.GH_TOKEN }}@github.com/BetterNCM/BetterNCM-Packed-Plugins
          git add .
          git commit -m "Update"
          git push
        working-directory: release