/* ============================================================
 * PlayBarHover — 启动脚本
 *
 * 全部逻辑都在 main.js（注入到主页面）。
 * 这个文件是 manifest 里 startup_script 指向的入口，
 * 保持为空操作即可，但不能删（否则 manifest 指向的文件不存在）。
 * ============================================================ */

// 无需在插件管理器侧做任何事
